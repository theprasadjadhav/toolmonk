apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: toolmonk-pdb
  namespace: toolmonk
spec:
  minAvailable: 1  # always keep at least 1 pod up during node drains / rolling updates
  selector:
    matchLabels:
      app: toolmonk
